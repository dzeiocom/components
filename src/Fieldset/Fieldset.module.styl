@import '../config.styl'

.fieldset
	border-radius 8px
	border 2px solid var(--gray-500)
	transition all $transition
	margin 0

	> legend
		font-weight 600
		transition all $transition
		padding 0 4px
		@media (prefers-color-scheme dark)
			color white
