@import "../config"

.code
.pre
	background nativeRGB(var(--theme-50))
	padding 4px 8px
	border-radius 8px
	@media (prefers-color-scheme dark)
		background nativeRGB(var(--gray-700))
		color white

.pre
	display block
	.code
		padding 0
